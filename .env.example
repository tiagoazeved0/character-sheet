# Copy to .env.local. Without these the app runs exactly as it did before phase 4:
# local-only, no account, no network. That is a supported state, not a broken one.
VITE_SUPABASE_URL=https://YOUR-PROJECT.supabase.co
VITE_SUPABASE_ANON_KEY=YOUR-ANON-KEY
